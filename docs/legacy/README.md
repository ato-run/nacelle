# Legacy docs

このディレクトリは、過去の構成（例: daemon 前提、`capsule` CLI 前提、`capsuled` 命名）に基づくドキュメントを保管します。

- 現行の挙動やビルド手順はルートの README と `cli/README.md` を優先してください。
- ここにある内容を現行に戻す場合は、`docs/adr/0001-cli-entrypoint.md` の決定と整合する形で更新してください。
