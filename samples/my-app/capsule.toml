# Capsule Manifest - UARC V1.1.0
schema_version = "1.0"
name = "my-app"
version = "0.1.0"
type = "app"

[metadata]
description = "Capsule generated from existing Node.js project"

[execution]
runtime = "source"
# Fallback (common)
entrypoint = "bun run src/index.ts"

[execution.dev]
entrypoint = "bun run --hot src/index.ts"

[execution.release]
entrypoint = "bun run src/index.ts"
