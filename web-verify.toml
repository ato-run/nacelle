[capsule]
name = "web-verify"
version = "0.0.1"

[runtime]
type = "native"
executable = "/bin/sh"
args = ["-c", "while true; do echo -e 'HTTP/1.1 200 OK\\r\\nContent-Length: 13\\r\\n\\r\\nHello Gumball' | nc -l -p $PORT; done"]
